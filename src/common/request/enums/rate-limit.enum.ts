export enum ENUM_RATE_LIMIT_TYPE {
    IP = 'ip',
    USER = 'user',
}

export enum ENUM_RATE_LIMIT_WINDOW {
    MINUTE = 'minute',
    HOUR = 'hour',
    DAY = 'day',
    WEEK = 'week',
    MONTH = 'month',
}
