export enum ENUM_MESSAGE_TYPE {
    TEXT = 'TEXT',
    IMAGE = 'IMAGE',
    VIDEO = 'VIDEO',
    FILE = 'FILE',
}

export enum ENUM_MESSAGE_STATUS {
    SENT = 'SENT',
    DELIVERED = 'DELIVERED',
    READ = 'READ',
}

export enum ENUM_CONVERSATION_TYPE {
    DIRECT = 'DIRECT',
}

export enum ENUM_USER_STATUS {
    ONLINE = 'ONLINE',
    OFFLINE = 'OFFLINE',
}
