export enum ENUM_SETTING_NAME {
    CURRENCY = 'currency',
    VAT = 'vat',
    POINT_PER = 'point_pair',
    DEFAULT_LOCATION = 'default_location',
}

export enum ENUM_SETTING_DATA_TYPE {
    BOOLEAN = 'BOOLEAN',
    STRING = 'STRING',
    NUMBER = 'NUMBER',
    POINT = 'POINT',
}
